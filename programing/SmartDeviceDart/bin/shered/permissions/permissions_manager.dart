// This class manage the permissions of user, both user permissions and user ability to give permissions
import 'permissions.dart';

class PermissionsManager {
  Permission permissions; // Save the permissions of the user
  Permission
      permissionsToGivePermissions; // Save the permissions of the user to give permissions to others

  PermissionsManager(this.permissionsToGivePermissions, this.permissions);

// Set

// Get

}
